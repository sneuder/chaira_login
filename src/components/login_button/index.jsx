import { ContainerButton, Button } from "./elements";

const LoginButton = () => {
  return (
    <ContainerButton>
      <Button>Ingresar</Button>
    </ContainerButton>
  );
};

export default LoginButton;